# 🚀 新功能快速上手

## 数据转换功能

### 1️⃣ JSON 转 CSV
```bash
ai-agent "@test/test_data.json 转换为CSV"
```

### 2️⃣ 验证 JSON 格式
```bash
ai-agent "@config.json 验证格式"
```

### 3️⃣ 美化 JSON
```bash
ai-agent "@data.json 美化格式"
```

### 4️⃣ YAML 转 JSON
```bash
ai-agent "@config.yaml 转换为JSON"
```

---

## 环境诊断功能

### 检查开发环境
```bash
ai-agent "检查开发环境"
```

### 诊断环境问题
```bash
ai-agent "诊断环境"
```

### 查看环境状态
```bash
ai-agent "环境检测"
```

---

## 组合使用示例

### 场景 1: 数据格式转换流程
```bash
# 1. 验证 JSON
ai-agent "@data.json 验证格式"

# 2. 如果正确，转换为 CSV
ai-agent "@data.json 转换为CSV"
```

### 场景 2: 环境问题排查
```bash
# 1. 先诊断环境
ai-agent "检查开发环境"

# 2. 根据建议安装依赖
pip install <missing-packages>

# 3. 再次检查
ai-agent "诊断环境"
```

---

## 完整示例

### 创建测试数据
```bash
cd test
cat > sample.json << EOF
[
  {"name": "Alice", "age": 30},
  {"name": "Bob", "age": 25}
]
EOF
```

### 执行转换
```bash
ai-agent "@test/sample.json 转换为CSV"
```

### 查看结果
```
✅ 数据转换成功

📄 源文件: test/sample.json
📊 格式: json → csv
📏 大小: 78 → 32 字符

转换结果:
────────────────────────────────────────
age,name
30,Alice
25,Bob
────────────────────────────────────────
```

---

## 更多信息

详细文档: [新功能使用指南](./new_features_guide.md)
