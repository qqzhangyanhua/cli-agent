# AI Agent 新功能使用指南

本文档介绍新增的**数据转换**和**环境诊断**功能的使用方法。

---

## 📊 数据转换功能

### 功能概述
支持多种数据格式之间的转换、验证和美化操作。

### 支持的格式
- **JSON** ↔ CSV
- **JSON** ↔ YAML  
- **CSV** → JSON
- **YAML** → JSON
- **XML** → JSON

### 支持的操作
1. **格式转换** (convert)
2. **格式验证** (validate)
3. **格式美化** (beautify)

---

### 使用示例

#### 1. JSON 转 CSV

```bash
# 引用文件并指定转换
ai-agent "@test/test_data.json 转换为CSV"

# 或
ai-agent "@test/test_data.json 转换成CSV格式"
```

**输出示例:**
```
✅ 数据转换成功

📄 源文件: test/test_data.json
📊 格式: json → csv
📏 大小: 345 → 89 字符

转换结果:
────────────────────────────────────────────────────────────────
id,name,age,city,skills
1,Alice,30,Beijing,"['Python', 'JavaScript', 'SQL']"
2,Bob,25,Shanghai,"['Java', 'Spring', 'MySQL']"
3,Charlie,35,Guangzhou,"['Go', 'Docker', 'Kubernetes']"
────────────────────────────────────────────────────────────────

💡 提示: 可以将结果保存到文件
```

#### 2. JSON 验证

```bash
ai-agent "@config.json 验证格式"

# 或
ai-agent "@data.json 检查JSON格式是否正确"
```

**输出示例 (正确):**
```
🔍 数据验证结果

📄 文件: config.json
📊 格式: json
🎯 结果: ✅ JSON格式正确
```

**输出示例 (错误):**
```
🔍 数据验证结果

📄 文件: config.json
📊 格式: json
🎯 结果: ❌ JSON格式错误: 第15行，第20列 - Expecting ',' delimiter

💡 提示: 请检查文件格式是否正确
```

#### 3. JSON 美化

```bash
ai-agent "@data.json 美化格式"

# 或
ai-agent "@config.json 格式化JSON"
```

**输出示例:**
```
✨ 格式美化完成

📄 文件: data.json
📊 格式: json
📏 大小: 250 → 385 字符

美化结果:
────────────────────────────────────────────────────────────────
{
  "name": "Test Project",
  "version": "1.0.0",
  "dependencies": {
    "requests": "^2.28.0",
    "flask": "^2.3.0"
  }
}
────────────────────────────────────────────────────────────────
```

#### 4. YAML 转 JSON

```bash
ai-agent "@config.yaml 转换为JSON"
```

#### 5. CSV 转 JSON

```bash
ai-agent "@data.csv 转换成JSON"
```

---

### 自动格式检测

系统会自动检测源文件格式，无需手动指定：

```bash
# 系统会自动识别是 JSON、YAML 还是 CSV
ai-agent "@myfile 转换为CSV"
```

---

## 🔍 环境诊断功能

### 功能概述
自动检测和诊断开发环境配置，帮助快速发现环境问题。

### 检查项目
1. **Python 环境**
   - Python 版本
   - pip 版本和配置
   - 虚拟环境状态

2. **项目依赖**
   - requirements.txt 解析
   - 缺失的依赖包
   - 版本不匹配的包

3. **开发工具**
   - Git 配置
   - Node.js/npm
   - Docker 状态

4. **系统资源**
   - 操作系统信息
   - 磁盘空间
   - 架构信息

---

### 使用示例

#### 1. 执行完整诊断

```bash
ai-agent "检查开发环境"

# 或
ai-agent "诊断环境"

# 或
ai-agent "环境检测"
```

**输出示例:**
```
🔍 环境诊断报告
================================================================================

✅ Python环境
   - Python版本: 3.12.2
   - pip: pip 25.1 from /usr/local/lib/python3.12/site-packages
   - 虚拟环境: ✅ 已激活

⚠️  项目依赖
   - 依赖文件: /project/requirements.txt
   - 缺失包: requests, pandas
   - 版本不符: numpy (需要>=1.20, 当前1.19.5)

✅ 开发工具
   - git: git version 2.39.5 ✓
   - node: v18.16.0 ✓
   - npm: 9.5.1 ✓
   - docker: Docker version 24.0.2 ✓

💻 系统资源
   - 操作系统: Darwin
   - 架构: arm64
   - 磁盘空间: 138.57 GB 可用 / 460.43 GB (69.9% 已使用)

📊 诊断总结
   - 发现问题: 3 个

❌ 问题列表:
   1. 缺失包: requests
   2. 缺失包: pandas
   3. 版本不符: numpy (需要>=1.20, 当前1.19.5)

💡 改进建议:
   1. 安装缺失包: pip install requests pandas
   2. 更新版本不符的包: pip install --upgrade numpy>=1.20

================================================================================
```

#### 2. 常见使用场景

**场景 1: 项目启动失败**
```bash
ai-agent "为什么我的项目跑不起来？先检查一下环境"
```

**场景 2: 新环境配置**
```bash
ai-agent "诊断环境并告诉我需要安装什么"
```

**场景 3: 定期检查**
```bash
ai-agent "检查开发环境是否正常"
```

---

## 💡 实用技巧

### 1. 组合使用 @ 文件引用

数据转换功能必须配合 `@` 文件引用使用：

```bash
# ✅ 正确 - 使用 @ 引用文件
ai-agent "@data.json 转换为CSV"

# ❌ 错误 - 没有引用文件
ai-agent "转换为CSV"  # 会提示需要引用文件
```

### 2. 支持相对路径和绝对路径

```bash
# 相对路径
ai-agent "@./test/data.json 转换为CSV"

# 绝对路径
ai-agent "@/Users/user/project/data.json 验证格式"

# 快速搜索（输入文件名的一部分）
ai-agent "@data 转换为CSV"  # 系统会搜索匹配的文件
```

### 3. 批量操作

```bash
# 可以引用多个文件依次处理
ai-agent "@file1.json 转换为CSV"
ai-agent "@file2.yaml 转换为JSON"
```

### 4. 结合其他功能

```bash
# 先检查环境，再执行其他操作
ai-agent "检查环境" && ai-agent "列出所有Python文件"
```

---

## 🚀 高级用法

### 自定义转换选项

虽然基本的转换通过自然语言指令即可完成，但你也可以通过程序化方式调用：

```python
from data_converter_tools import data_converter_tools

# 转换
result = data_converter_tools.convert(
    content=json_content,
    source_format="json",
    target_format="csv",
    file_path="data.json"
)

# 验证
result = data_converter_tools.validate(content, "json")

# 美化
result = data_converter_tools.beautify(content, "json")
```

### 自定义环境诊断

```python
from env_diagnostic_tools import env_diagnostic_tools

# 完整诊断
result = env_diagnostic_tools.full_diagnostic()

# 格式化报告
report_text = env_diagnostic_tools.format_report(result["report"])
```

---

## ❓ 常见问题

### Q1: 为什么转换提示"未找到文件"？
**A:** 确保使用 `@` 符号引用文件，例如 `@data.json` 而不是 `data.json`

### Q2: 支持哪些数据格式？
**A:** 目前支持 JSON、YAML、CSV、XML。更多格式正在开发中。

### Q3: 可以转换很大的文件吗？
**A:** 建议文件大小在 10MB 以内，过大的文件可能会导致响应缓慢。

### Q4: 环境诊断会修改我的配置吗？
**A:** 不会。环境诊断只读取信息，不会修改任何配置。

### Q5: 如何保存转换结果？
**A:** 转换结果会显示在终端中，你可以复制后保存，或者使用输出重定向：
```bash
ai-agent "@data.json 转换为CSV" > output.csv
```

---

## 📚 相关文档

- [项目概览](../CLAUDE.md)
- [文件引用功能](../docs/file-reference-feature.md)
- [Git 工具集成](../docs/git-integration.md)
- [待办事项管理](../docs/todo-management.md)

---

## 🔄 版本历史

### v1.1.0 (2024-01-20)
- ✨ 新增数据转换功能
- ✨ 新增环境诊断功能
- 🐛 修复文件引用解析问题

---

## 💬 反馈与建议

如有问题或建议，请提交 Issue 或 Pull Request。
