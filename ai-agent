#!/usr/bin/env python3
"""
NM智能体终端控制工具 - CLI版本

一个强大的 AI 终端助手，支持自然语言执行命令、文件操作、数据转换、环境诊断、
待办管理和对话记忆等多种智能功能。

🎯 核心功能:
- 🗣️ 自然语言执行命令 - 用人话说，让 AI 执行终端命令
- 📁 @ 智能文件引用 - 交互式文件选择器，快速引用文件
- 🧠 对话记忆 - 记住上下文，支持连续对话
- 📝 Git 智能工具 - 自动生成 commit 消息、代码审查
- 📊 数据转换工具 - JSON/CSV/YAML/XML 格式互转、验证
- 🔍 环境诊断 - 自动检测开发环境配置和依赖问题
- 📋 待办事项管理 - 智能识别并管理日程安排和任务提醒

使用示例:
    # 基础功能
    ai-agent                              # 交互模式
    ai-agent "列出所有Python文件"          # 单次执行
    ai-agent --help                       # 查看详细帮助

    # 文件引用和数据转换
    ai-agent "@README.md 这是做什么的？"    # 引用文件并询问
    ai-agent "@data.json 转换为CSV"       # 数据格式转换
    ai-agent "@config.yaml 验证格式"      # 格式验证

    # 智能功能
    ai-agent "生成commit消息"             # Git智能提交
    ai-agent "对当前代码进行code review"   # 代码审查
    ai-agent "检查开发环境"               # 环境诊断

    # 待办事项管理
    ai-agent "今天18点给陈龙打电话"        # 添加待办
    ai-agent "明天上午10点开会"           # 添加待办
    ai-agent "今天有什么要做的"            # 查询待办
    ai-agent "搜索陈龙相关的待办"          # 搜索待办
"""

import sys
import os
import argparse
from pathlib import Path

# 添加项目目录到Python路径（支持从任何位置运行）
SCRIPT_DIR = Path(__file__).parent.absolute()
if str(SCRIPT_DIR) not in sys.path:
    sys.path.insert(0, str(SCRIPT_DIR))

from agent_config import AgentState
from agent_memory import memory
from agent_workflow import build_agent
from agent_ui import print_header, handle_special_commands
from file_reference_parser import update_working_directory
from interactive_file_selector import (
    interactive_file_select,
    quick_file_select,
    update_selector_working_directory,
)

__version__ = "1.0.0"


def smart_input_handler(prompt: str = "👤 你: ") -> str:
    """智能输入处理器，支持 @ 文件选择"""
    while True:
        try:
            user_input = input(prompt).strip()

            # 检查是否只输入了 @
            if user_input == "@":
                print("\n🎯 启动文件选择器...")
                selected_file = interactive_file_select("选择要引用的文件")
                if selected_file:
                    # 重新提示用户输入，并预填充选择的文件
                    print(f"\n✅ 已选择文件: @{selected_file}")
                    new_prompt = f"👤 你 (已选择 @{selected_file}): "
                    additional_input = input(new_prompt).strip()
                    if additional_input:
                        return f"@{selected_file} {additional_input}"
                    else:
                        return f"读取 @{selected_file}"
                else:
                    print("🔄 继续输入...")
                    continue

            # 检查是否以 @ 开头但不完整
            elif user_input.startswith("@") and len(user_input) > 1:
                # 提取 @ 后的部分作为搜索条件
                search_term = user_input[1:].split()[0]  # 取第一个词作为文件名
                remaining_text = user_input[len(search_term) + 1 :].strip()

                # 如果搜索词看起来不完整（短或没有路径分隔符），提供快速选择
                # 改为 <= 10，让更多的输入都能触发文件选择
                if len(search_term) <= 10 and not any(c in search_term for c in "./\\"):
                    print(f"\n🔍 搜索文件: '{search_term}'...")
                    selected_file = quick_file_select(search_term)
                    if selected_file:
                        if remaining_text:
                            return f"@{selected_file} {remaining_text}"
                        else:
                            # 询问用户要做什么
                            action = input(f"👤 对 @{selected_file} 要做什么? ").strip()
                            if action:
                                return f"@{selected_file} {action}"
                            else:
                                return f"读取 @{selected_file}"
                    else:
                        print("🔄 继续输入...")
                        continue

            # 普通输入，直接返回
            return user_input

        except (KeyboardInterrupt, EOFError):
            print("\n\n👋 检测到中断信号，退出程序...\n")
            sys.exit(0)


def parse_arguments():
    """解析命令行参数"""

    # 创建详细的帮助信息
    help_text = """AI智能体终端控制工具 - 使用自然语言执行终端命令

🎯 核心功能:
  🗣️  自然语言执行命令    - 用人话说，让 AI 执行终端命令
  📁  @ 智能文件引用      - 交互式文件选择器，快速引用文件
  🧠  对话记忆           - 记住上下文，支持连续对话
  📝  Git 智能工具       - 自动生成 commit 消息、代码审查
  📊  数据转换工具       - JSON/CSV/YAML/XML 格式互转、验证
  🔍  环境诊断          - 自动检测开发环境配置和依赖问题
  📋  待办事项管理       - 智能识别并管理日程安排和任务提醒

💡 使用示例:
  基础命令:
    ai-agent                              # 交互模式
    ai-agent "列出所有Python文件"          # 单次执行
    ai-agent "显示git状态"                # Git操作
    
  文件引用:
    ai-agent "@README.md 这是做什么的？"    # 引用文件并询问
    ai-agent "@data.json 转换为CSV"       # 数据格式转换
    
  智能功能:
    ai-agent "生成commit消息"             # Git智能提交
    ai-agent "检查开发环境"               # 环境诊断
    ai-agent "今天18点给陈龙打电话"        # 添加待办事项
    ai-agent "今天有什么要做的"            # 查询待办事项

🎨 交互模式特殊命令:
    tools/工具    - 查看MCP工具列表
    models/模型   - 查看双LLM配置
    files/@       - 文件引用功能说明
    todos/待办    - 查看今日待办
    help/帮助     - 显示功能帮助
    history/历史  - 查看对话历史
    clear/清空    - 清空对话历史
    exit/quit     - 退出程序"""

    parser = argparse.ArgumentParser(
        prog="ai-agent",
        description=help_text,
        formatter_class=argparse.RawDescriptionHelpFormatter,
    )

    parser.add_argument(
        "command", nargs="*", help="要执行的命令（可选，不提供则进入交互模式）"
    )

    parser.add_argument(
        "-v", "--version", action="version", version=f"%(prog)s {__version__}"
    )

    parser.add_argument(
        "-i", "--interactive", action="store_true", help="强制进入交互模式"
    )

    parser.add_argument(
        "-q", "--quiet", action="store_true", help="安静模式，不显示欢迎信息"
    )

    parser.add_argument("--no-memory", action="store_true", help="禁用对话记忆功能")

    parser.add_argument(
        "-w",
        "--working-dir",
        type=str,
        default=os.getcwd(),
        help="设置工作目录（默认：当前目录）",
    )

    return parser.parse_args()


def execute_single_command(command: str, quiet: bool = False) -> int:
    """
    执行单条命令

    Args:
        command: 要执行的命令
        quiet: 是否安静模式

    Returns:
        退出码：0表示成功，1表示失败
    """
    try:
        # 构建智能体
        agent = build_agent()

        if not quiet:
            print(f"\n🤖 执行命令: {command}\n")

        # 构建初始状态
        initial_state: AgentState = {
            "user_input": command,
            "intent": "unknown",
            "command": "",
            "commands": [],
            "command_output": "",
            "command_outputs": [],
            "response": "",
            "error": "",
            "needs_file_creation": False,
            "file_path": "",
            "file_content": "",
            "chat_history": [],
            "mcp_tool": "",
            "mcp_params": {},
            "mcp_result": "",
            "original_input": "",
            "referenced_files": [],
            "file_contents": {},
            # 待办字段仅在需要时由节点填充，不预先初始化
            "todo_action": "",
            "todo_date": "",
            "todo_time": "",
            "todo_content": "",
            "todo_result": "",
            # 数据转换字段
            "data_conversion_type": "",
            "source_format": "",
            "target_format": "",
            "conversion_result": "",
            # 环境诊断字段
            "diagnostic_result": "",
        }

        # 执行工作流
        result = agent.invoke(initial_state)

        # 显示结果
        # 注意：问答节点（question_answerer）已经流式打印了，不需要重复显示
        intent = result.get("intent", "")

        if quiet:
            # 安静模式：只输出结果，不带格式
            if intent != "question":  # 问答已流式打印
                print(result["response"])
        else:
            # 非问答类型需要格式化输出
            if intent not in ["question"]:
                print("─" * 80)
                print(f"🤖 助手: {result['response']}")
                print("─" * 80)

        # 判断是否成功
        if result.get("error"):
            return 1
        return 0

    except KeyboardInterrupt:
        print("\n\n👋 执行已取消\n")
        return 130  # SIGINT
    except Exception as e:
        print(f"\n❌ 错误: {str(e)}\n", file=sys.stderr)
        return 1


def interactive_mode(quiet: bool = False, no_memory: bool = False):
    """
    交互模式

    Args:
        quiet: 是否安静模式
        no_memory: 是否禁用记忆
    """
    # 打印欢迎信息
    if not quiet:
        print_header()

    # 构建智能体
    agent = build_agent()

    if not quiet:
        print("🎬 准备就绪！请输入你的指令或问题...\n")

    while True:
        try:
            # 使用智能输入处理器获取用户输入
            user_input = smart_input_handler("👤 你: ")

            if not user_input:
                continue

            # 处理特殊命令
            special_result = handle_special_commands(user_input)
            if special_result is True:  # 退出
                break
            elif special_result is False:  # 已处理，继续循环
                continue

            print()  # 空行

            # 构建初始状态
            initial_state: AgentState = {
                "user_input": user_input,
                "intent": "unknown",
                "command": "",
                "commands": [],
                "command_output": "",
                "command_outputs": [],
                "response": "",
                "error": "",
                "needs_file_creation": False,
                "file_path": "",
                "file_content": "",
                "chat_history": [],
                "mcp_tool": "",
                "mcp_params": {},
                "mcp_result": "",
                "original_input": "",
                "referenced_files": [],
                "file_contents": {},
                "todo_action": "",
                "todo_date": "",
                "todo_time": "",
                "todo_content": "",
                "todo_result": "",
                "data_conversion_type": "",
                "source_format": "",
                "target_format": "",
                "conversion_result": "",
                "diagnostic_result": "",
            }

            # 执行工作流
            result = agent.invoke(initial_state)

            # 显示响应
            # 注意：问答节点已经流式打印，其他类型需要格式化输出
            intent = result.get("intent", "unknown")

            if intent not in ["question"]:
                print("─" * 80)
                print(f"🤖 助手: {result['response']}")
                print("─" * 80 + "\n")
            else:
                # 问答已经流式输出，只需要空行分隔
                print()

            # 保存到记忆（除非禁用）
            if not no_memory:
                memory.add_interaction(
                    user_input, result["response"], result.get("intent", "unknown")
                )

        except KeyboardInterrupt:
            print("\n\n👋 检测到中断信号，退出程序...\n")
            break
        except Exception as e:
            print(f"\n❌ 发生错误: {str(e)}\n", file=sys.stderr)
            print("请重试或输入 'exit' 退出\n")


def main():
    """主函数"""
    args = parse_arguments()

    # 设置工作目录
    if args.working_dir:
        try:
            os.chdir(args.working_dir)
            # 同步文件引用解析器的工作目录
            update_working_directory(args.working_dir)
            update_selector_working_directory(args.working_dir)
            if not args.quiet:
                print(f"📂 工作目录: {os.getcwd()}")
        except Exception as e:
            print(f"❌ 无法切换到目录 {args.working_dir}: {e}", file=sys.stderr)
            return 1
    else:
        # 确保文件引用解析器使用当前目录
        current_dir = os.getcwd()
        update_working_directory(current_dir)
        update_selector_working_directory(current_dir)

    # 如果禁用记忆，清空记忆
    if args.no_memory:
        memory.clear()

    # 判断模式
    if args.command and not args.interactive:
        # 单次命令模式 - 将命令列表用空格连接
        command_str = " ".join(args.command)
        exit_code = execute_single_command(command_str, args.quiet)
        return exit_code
    else:
        # 交互模式
        interactive_mode(args.quiet, args.no_memory)
        return 0


if __name__ == "__main__":
    sys.exit(main())
